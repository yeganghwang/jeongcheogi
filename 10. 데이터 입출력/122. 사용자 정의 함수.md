# 1. 사용자 정의 함수 개요
> 직접 만들어 사용하는 함수.

C
``` C
#include <stdio.h>
int factorial(int n);

int main(void) {
    int (*pf)(int);
    pf = factorial;
    printf("%d", pf(3));
}

int factorial(int n) {
    if (n <= 1)
        return 1;
    else
        return n * factorial(n-1);
}
```