# 형상 관리
> 식통감기

소프트웨어 개발 과정에서 산출물의 변경 사항을 체계적으로 관리하는 절차
- `형상식별`: 관리할 항목 정의
- `형상통제`: 변경 요청 관리 및 승인
- `형상감사`: 변경 이력 및 산출물 상태 점검
- `형상기록`: 형상 항목과 변경 사항 문서화

# 정규화 단계
> 도부이결다조

데이터 중복을 줄이고 이상 현상을 방지하기 위해 관계형 데이터베이스 설계 기법
- `도메인 분해`: 컬럼 값이 유효한 도메인에 속하는지
- `부분 함수 종속 제거`: 기본키 일부에만 종속된 속성 제거
- `이행 함수 종속 제거`: 비주요 속성이 다른 비주요 속성에 종속된 것 제거
- `결정자가 후보키 아닌 것 제거`: 
- `다치 종속 제거`: 하나의 속성이 여러 값 가질 때 다른 속성들과 독립적인 경우 분리
- `조인 종속 제거`: 조인으로만 복원 가능한 관계 제거

# OSI 7계층
> 응표세전네데물

1. 응용 계층: 사용자 인터페이스 제공
1. 표현 계층: 데이터 형식 변환 및 암호화
1. 세션 계층: 연결 설정, 유지, 종료
1. 전송 계층: 신뢰성 있는 데이터 전송
1. 네트워크 계층: 경로 설정 및 IP 주소
1. 데이터링크 계층: 물리 주소(MAC) 기반 데이터 전송
1. 물리 계층: 전기적/물리적 신호 전달

# XP 가치
> 의사피존용단
- 의사소통
- 피드백
- 존중
- 용기
- 단순성

# 결합도
> 내공외제스자
- `내용 결합도(최악)`: 내부 구조를 직접 참조
- `공통 결합도`: 전역변수 공유
- `외부 결합도`: 외부 인터페이스 공유
- `제어 결합도`: 제어 정보를 인자로 전달
- `스탬프 결합도`: 구조체 일부만 사용
- `자료 결합도(최선)`: 필요한 데이터만 인자로 전달

# 응집도
> 우논시절통순기
- `우연적 응집도`: 관련 없는 작업 묶음
- `논리적 응집도`: 논리적으로 유사한 기능 묶음
- `시간적 응집도`: 특정 시간에 실행될 작업 묶음
- `절차적 응집도`: 순서대로 실행될 기능 묶음
- `통신적 응집도`: 동일한 데이터 사용
- `순차적 응집도`: 한 작업의 결과가 다음 작업의 입력
- `기능적 응집도(최선)`: 하나의 기능에 집중

# 요구사항 개발 프로세스
> 도분명확
1. 도출
1. 분석
1. 명세
1. 확인

# 객체지향 분석 기법
럼바우-객동기
1. 객체 모델링: 클래스와 속성, 관계 표현
1. 동적 모델링: 상태 변화 표현
1. 기능 모델링: 데이터 흐름 및 기능 표현

# 아키텍처 4+1 관점
> 논구프배유
1. 논리적: 클래스, 객체
1. 구현: 소스코드 구조
1. 프로세스: 동시성, 스레드
1. 배치: 실행환경
1. 유스케이스: 사용자 요구사항

# 정적 다이어그램
> 클객컴배복패
- `클래스 다이어그램`: 클래스 구조
- `객체 다이어그램`: 클래스의 인스턴스
- `컴포넌트 다이어그램`: 시스템의 컴포넌트 구조
- `배치 다이어그램`: 물리적 아키텍처
- `복합적 다이어그램`: 클래스 내부 구조 표현 
- `패키지 다이어그램`: 패키지 간의 관계 표현

# 동적 다이어그램
> 유시커상활타
- 유스케이스, 시퀀스, 커뮤니케이션, 상태, 활동, 타이밍

# Cron
분 시 일 월 요일

# EAI
기업 애플리케이션 통합 방식
1. Point to Point: 1:1연결
1. Hub&Spoke: 중앙 허브 중계
1. Message Bus: 메시지 버스 활용
1. Hybrid: 혼합 구조

# SOLID 원칙
객체지향 5대 원칙
1. Single Responsibility: 하나의 책임만
1. Open/Closed: 확장엔 열려 있고 수정엔 닫힘
1. Liscov Substitution: 자식 클래스는 부모 대체 가능
1. Interface Segregation: 클라이언트는 자신이 사용하지 않는 메서드에 의존하지 않는다.
1. Dependency Inversion: 고수준 모듈은 저수준 모듈에 의존하지 않는다.

# 디자인 패턴
> 생구행
## 생성 패턴
> 생빌프팩앱싱
- Builder: 복잡한 객체의 생성 과정을 단계별로 분리
- Prototype: 객체를 복제하여 새로운 객체 생성
- Factory Method: 객체 생성을 서브클래스에 위임
- Abstract Factory: 관련 객체들을 생성하는 인터페이스 제공
- Singleton: 하나의 인스턴스를 하나만 생성

## 구조 패턴
> 구어데브프브플컴프
- `Adapter`: 호환되지 않는 인터페이스를 연결
- `Decorater`: 객체에 동적으로 기능 추가
- `Facade`: 복잡한 서브시스템에 대한 간단한 인터페이스 제공
- `Bridge`: 구현부와 추상부를 분리하여 독립적으로 확장
- `Flyweight`: 공유를 통해 많은 객체를 효율적으로 지원
- `Composite`: 객체들을 트리 구조로 구성하여 부분-전체 계층 표현
- `Proxy`: 다른 객체에 대한 접근을 제어

## 행위 패턴
- `Strategy`: 알고리즘을 캡슐화하여 교체 가능하게 만듦
- `Temple Method`: 알고리즘의 구조를 정의하고 일부 단계를 서브클래스에 위임
- `Mediator`: 객체 간의 복잡한 상호작용을 캡슐화
- `Memento`: 객체의 상태를 저장하고 복원
- `Observer`: 객체의 상태 변화를 다른 객체에게 알림
- `State`: 객체의 내부 상태에 따라 행동을 변경
- `Visitor`: 객체 구조에 대한 연산을 분리
- `Command`: 요청을 객체로 캡슐화
- `Iterator`: 컬렉션의 요소에 접근하는 방법을 제공
- `Chain of Responsibility`: 요청을 처리할 수 있는 객체들을 연결
- `Interpreter`: 문법을 정의하고 해석

# 테스트 기준
> 다선조결 결조 구
- `다중조건 검증`: 모든 조건 조합 검증
- `선택검증`: 조건문의 각 분기 검증
- `조건검증`: 각 조건의 참/거짓 검증
- `결정검증`: 전체 조건문의 참/거짓 검증
- `결정/조건 검증`: 결정과 조건 모두 검증
- `구문 검증`: 모든 실행문 검증

# 소스코드 최적화
> 가단의중추
- 가독성 향상
- 단순 구조
- 의존 최소화
- 중복 제거
- 추상화로 재사용성 향상

# ISO/IEC 9126
소프트웨어 품질 특성
> 기신사효유이
- `기능성`
- `신뢰성`
- `사용성`
- `효율성`
- `유지보수성`
- `이식성`

# ISO/IEC 14598
소프트웨어 품질 평가 기준
> 반재공객
- `반복성`
- `재현성`
- `공정성`
- `객관성`

# DAC/MAC 비교
|항목|DAC|MAC|
|---|---|---|
|권한 결정 주체|리소스 공유자|시스템 정책|
|유연성|높음|낮음|
|보안성|낮음|높음|
|대표기술|`chmod`, `chown`|SELinux, AppArmor|
|실사용 예시|일반 리눅스 시스템|정부, 군 시스템|

# UML 관계
> 연집 합의일실
1. 연관 관계
1. 집합 관계
1. 합성 관계
1. 의존 관계
1. 일반화 관계
1. 실체화 관계